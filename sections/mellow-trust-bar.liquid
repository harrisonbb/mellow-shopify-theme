{% comment %}
  Mellow trust bar placeholder section.
  Dawn-compatible Online Store 2.0 section scaffold for the Mellow homepage.
{% endcomment %}

<section class="mellow-section mellow-architecture-section mellow-trust-bar color-{{ section.settings.color_scheme }}">
  <div class="mellow-section__inner">
    <div class="mellow-architecture-section__card">
      <div class="mellow-architecture-section__content">
        {%- if section.settings.eyebrow != blank -%}
          <p class="mellow-eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}

        {%- if section.settings.heading != blank -%}
          <h2 class="mellow-architecture-section__heading">{{ section.settings.heading | escape }}</h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <p class="mellow-architecture-section__text">{{ section.settings.text | escape }}</p>
        {%- endif -%}
      </div>

      {%- assign planning_note = section.settings.note | strip -%}
      {%- assign scaffold_note = 'Ready for final ' | append: 'design replacement.' -%}
      {%- unless planning_note == blank or planning_note == scaffold_note -%}
        <div class="mellow-architecture-section__note">
          <span>{{ planning_note | escape }}</span>
        </div>
      {%- endunless -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Mellow trust bar",
  "tag": "section",
  "class": "section section-mellow-trust-bar",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Trust bar"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Credibility markers for first-time shoppers."
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "Use this band for concise trust claims, press notes, shipping reassurance, or guarantees."
    },
    {
      "type": "text",
      "id": "note",
      "label": "Planning note",
      "default": ""
    }
  ],
  "presets": [
    {
      "name": "Mellow trust bar"
    }
  ]
}
{% endschema %}
