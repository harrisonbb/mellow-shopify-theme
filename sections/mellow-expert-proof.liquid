{% comment %}
  Mellow Expert Proof section.
  Built on the global Mellow section system with a native scroll carousel.
{% endcomment %}

<style>
  #MellowExpertProof-{{ section.id }} {
    --mellow-orange: var(--mellow-orange-red, #D04A2A);
    --mellow-expert-proof-bg: var(--mellow-section-green, var(--mellow-forest, #123308));
    --mellow-expert-proof-shelf: var(--mellow-ivory, #FCFBF7);
    --mellow-expert-proof-surface: var(--mellow-cream, #FCFBF7);
    --mellow-expert-proof-gap: clamp(24px, 2vw, 36px);
    --mellow-expert-proof-card-width: calc((100% - (var(--mellow-expert-proof-gap) * 3)) / 4);
    background: var(--mellow-expert-proof-shelf);
    color: var(--mellow-forest, #123308);
    scroll-margin-top: calc(var(--header-height, 68px) + 12px);
    overflow-x: hidden;
    overflow-y: visible;
    padding: 0;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__band {
    width: 100%;
    background:
      radial-gradient(circle at 54% 45%, rgba(3, 95, 34, 0.72) 0%, rgba(3, 77, 29, 0.38) 24%, rgba(18, 51, 8, 0) 54%),
      linear-gradient(90deg, #092c12 0%, var(--mellow-expert-proof-bg) 48%, #0b3214 100%);
    color: var(--mellow-cream, #FCFBF7);
    min-height: clamp(310px, 21vw, 340px);
    display: flex;
    align-items: center;
    box-sizing: border-box;
    padding: clamp(42px, 3.4vw, 54px) 0;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__shelf {
    width: 100%;
    background: var(--mellow-expert-proof-shelf);
    padding-top: clamp(38px, 2.8vw, 46px);
    padding-bottom: clamp(64px, 5vw, 92px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
    grid-template-columns: minmax(0, 1.38fr) minmax(360px, 1fr);
    align-items: center;
    gap: clamp(42px, 4.4vw, 70px);
    margin-bottom: 0;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header {
    max-width: none;
  }

  #MellowExpertProof-{{ section.id }} .mellow-eyebrow {
    margin-bottom: clamp(20px, 1.5vw, 26px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__heading {
    max-width: 760px;
    color: var(--mellow-cream, #FCFBF7);
    font-size: clamp(48px, 4.45vw, 72px);
    line-height: 1.04;
    letter-spacing: 0;
    text-wrap: balance;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__actions {
    align-self: center;
    justify-self: end;
    width: 100%;
    min-height: clamp(190px, 16vw, 220px);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-left: clamp(44px, 4vw, 68px);
    border-left: 1px solid rgba(240, 235, 224, 0.30);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__text {
    max-width: 520px;
    margin: 0;
    font-size: clamp(19px, 1.56vw, 24px);
    line-height: 1.46;
    letter-spacing: 0;
    color: rgba(252, 251, 247, 0.88);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__action-row {
    display: flex;
    align-items: center;
    gap: clamp(22px, 2.25vw, 34px);
    width: auto;
    margin-top: clamp(34px, 2.8vw, 46px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__cta {
    --mellow-section-cta-bg: var(--mellow-cream, #FCFBF7);
    --mellow-section-cta-border: var(--mellow-cream, #FCFBF7);
    --mellow-section-cta-color: var(--mellow-forest, #123308);
    --mellow-section-cta-hover-bg: var(--mellow-orange-red, #D04A2A);
    --mellow-section-cta-hover-border: var(--mellow-orange-red, #D04A2A);
    --mellow-section-cta-hover-color: var(--mellow-cream, #FCFBF7);
    --mellow-section-cta-width: clamp(230px, 17vw, 268px);
    min-height: 72px;
    font-size: clamp(18px, 1.24vw, 22px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__cta .mellow-section-cta__arrow {
    display: none;
  }

  #MellowExpertProof-{{ section.id }} .mellow-section-cta-wrap {
    flex: 0 0 auto;
    width: auto;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrows {
    display: flex;
    align-items: center;
    gap: clamp(20px, 1.7vw, 30px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 72px;
    padding: 0;
    border: 1px solid rgba(252, 251, 247, 0.72);
    border-radius: 999px;
    background: transparent;
    color: var(--mellow-cream, #FCFBF7);
    cursor: pointer;
    box-shadow: none;
    transition: background-color 180ms ease, border-color 180ms ease, color 180ms ease;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:not(:disabled):hover,
  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:not(:disabled):focus-visible {
    border-color: var(--mellow-cream, #FCFBF7);
    background: var(--mellow-cream, #FCFBF7);
    color: var(--mellow-forest, #123308);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:disabled {
    cursor: default;
    opacity: 0.42;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow svg {
    width: 20px;
    height: 20px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__bleed {
    width: 100%;
    overflow-x: hidden;
    overflow-y: visible;
    background: var(--mellow-expert-proof-shelf);
    position: relative;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__scroller {
    width: 100%;
    overflow-x: auto;
    overflow-y: visible;
    scrollbar-width: none;
    box-sizing: border-box;
    padding: 0 0 4px;
    scroll-padding-inline: 0;
    scroll-behavior: smooth;
    scroll-snap-type: x proximity;
    -webkit-overflow-scrolling: touch;
    background: var(--mellow-expert-proof-shelf);
  }

  #MellowExpertProof-{{ section.id }}.is-reduced-motion .mellow-expert-proof__scroller {
    scroll-behavior: auto;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__scroller::-webkit-scrollbar {
    display: none;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__track {
    display: flex;
    gap: var(--mellow-expert-proof-gap);
    align-items: stretch;
    width: 100%;
    min-width: 100%;
    background: var(--mellow-expert-proof-shelf);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__card {
    flex: 0 0 var(--mellow-expert-proof-card-width);
    width: var(--mellow-expert-proof-card-width);
    display: flex;
    flex-direction: column;
    min-height: clamp(520px, 36vw, 560px);
    min-width: 0;
    overflow: hidden;
    isolation: isolate;
    scroll-snap-align: start;
    border-radius: 16px;
    border: 1px solid var(--mellow-card-border, rgba(18, 51, 8, 0.10));
    background: var(--mellow-expert-proof-surface);
    box-shadow: 0 20px 42px rgba(18, 51, 8, 0.15);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    height: clamp(220px, 15vw, 250px);
    overflow: hidden;
    border-radius: 16px 16px 0 0;
    background:
      radial-gradient(circle at 24% 14%, rgba(208, 74, 42, 0.18), transparent 32%),
      linear-gradient(135deg, #DCE4DF 0%, #F8F4EC 58%, #E8D5C2 100%);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__image {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center 28%;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__letter {
    color: rgba(18, 51, 8, 0.34);
    font-family: var(--mellow-font-display);
    display: block;
    font-size: clamp(44px, 4.5vw, 72px);
    font-weight: 650;
    line-height: .78;
    letter-spacing: 0;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__content {
    display: flex;
    flex-direction: column;
    flex: 1 1 auto;
    padding: 22px 24px 26px;
    border-radius: 0 0 16px 16px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__role {
    display: block;
    margin: 0 0 clamp(12px, .9vw, 16px);
    color: var(--mellow-orange, #D04A2A);
    font-size: clamp(10px, .74vw, 12px);
    font-weight: var(--mellow-eyebrow-weight, 700);
    line-height: var(--mellow-eyebrow-line-height, 1);
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__quote {
    margin: 0;
    color: var(--mellow-forest, #123308);
    font-size: clamp(14px, 0.9vw, 16px);
    line-height: 1.58;
    letter-spacing: 0;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__person {
    margin-top: auto;
    padding-top: clamp(22px, 1.8vw, 28px);
    border-top: 1px solid rgba(18, 51, 8, 0.13);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__name {
    margin: 0;
    color: var(--mellow-forest, #123308);
    font-size: clamp(18px, 1.2vw, 22px);
    line-height: 1.02;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__title {
    margin: 6px 0 0;
    color: rgba(18, 51, 8, 0.64);
    font-size: clamp(14px, .95vw, 16px);
    line-height: 1.22;
  }

  @media screen and (min-width: 990px) and (max-height: 820px) {
    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__band {
      min-height: 300px;
      padding-top: 36px;
      padding-bottom: 36px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__shelf {
      padding-top: 38px;
      padding-bottom: 64px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__card {
      min-height: 500px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual {
      height: clamp(210px, 14vw, 230px);
    }
  }

  @media screen and (max-width: 989px) {
    #MellowExpertProof-{{ section.id }} {
      --mellow-expert-proof-gap: clamp(18px, 2.6vw, 28px);
      --mellow-expert-proof-card-width: clamp(300px, 44vw, 340px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__band {
      min-height: auto;
      padding-top: clamp(50px, 7vw, 72px);
      padding-bottom: clamp(50px, 7vw, 72px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
      grid-template-columns: 1fr;
      gap: clamp(24px, 4vw, 36px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__actions {
      min-height: auto;
      padding-left: 0;
      border-left: 0;
      justify-self: start;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__action-row,
    #MellowExpertProof-{{ section.id }} .mellow-section-cta-wrap {
      justify-content: flex-start;
    }
  }

  @media screen and (max-width: 749px) {
    #MellowExpertProof-{{ section.id }} {
      --mellow-expert-proof-gap: 18px;
      --mellow-expert-proof-card-width: min(84vw, 330px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__band {
      padding-top: 42px;
      padding-bottom: 42px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
      gap: 22px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__heading {
      font-size: clamp(36px, 9.5vw, 46px);
      line-height: 1.04;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__action-row {
      flex-wrap: wrap;
      width: 100%;
      gap: 16px;
      margin-top: 22px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__cta {
      --mellow-section-cta-width: min(100%, 260px);
      min-height: 60px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow {
      width: 50px;
      height: 50px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__shelf {
      padding-top: 28px;
      padding-bottom: 54px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__card {
      min-height: 500px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual {
      height: 184px;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__content {
      padding: 20px 20px 24px;
    }
  }
</style>

<section id="MellowExpertProof-{{ section.id }}" class="mellow-section-standard mellow-expert-proof color-{{ section.settings.color_scheme }}" data-mellow-expert-proof>
  <div class="mellow-expert-proof__band">
    <div class="mellow-section-axis">
      <div class="mellow-section-header-grid mellow-expert-proof__header-grid">
        <div class="mellow-section-header mellow-expert-proof__header">
          {%- if section.settings.eyebrow != blank -%}
            <p class="mellow-eyebrow">{{ section.settings.eyebrow | escape }}</p>
          {%- endif -%}

          {%- if section.settings.heading != blank -%}
            {%- assign heading_lines = section.settings.heading | escape | split: '|' -%}
            <h2 class="mellow-section-heading mellow-expert-proof__heading">
              {%- for line in heading_lines -%}
                {{ line }}{%- unless forloop.last -%}<br>{%- endunless -%}
              {%- endfor -%}
            </h2>
          {%- endif -%}

        </div>

        <div class="mellow-section-actions mellow-expert-proof__actions">
          {%- if section.settings.text != blank -%}
            {%- assign text_lines = section.settings.text | escape | split: '|' -%}
            <p class="mellow-section-text mellow-expert-proof__text">
              {%- for line in text_lines -%}
                {{ line }}{%- unless forloop.last -%}<br>{%- endunless -%}
              {%- endfor -%}
            </p>
          {%- endif -%}

          <div class="mellow-expert-proof__action-row">
            {%- if section.settings.cta_label != blank -%}
              <div class="mellow-section-cta-wrap">
                <a class="mellow-section-cta mellow-expert-proof__cta" href="{{ section.settings.cta_link | default: '#' }}">
                  <span class="mellow-section-cta__label">{{ section.settings.cta_label | escape }}</span>
                  <span class="mellow-section-cta__arrow" aria-hidden="true">→</span>
                </a>
              </div>
            {%- endif -%}

            <div class="mellow-expert-proof__arrows" aria-label="Expert proof scroll controls">
              <button class="mellow-expert-proof__arrow" type="button" aria-label="Previous expert quote" aria-disabled="false" data-expert-prev>
                <svg viewBox="0 0 16 16" fill="none" aria-hidden="true" focusable="false">
                  <path d="M10 3.5 5.5 8 10 12.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </button>
              <button class="mellow-expert-proof__arrow" type="button" aria-label="Next expert quote" data-expert-next>
                <svg viewBox="0 0 16 16" fill="none" aria-hidden="true" focusable="false">
                  <path d="m6 3.5 4.5 4.5L6 12.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="mellow-expert-proof__shelf">
    <div class="mellow-section-axis">
      <div class="mellow-expert-proof__bleed">
        <div class="mellow-expert-proof__scroller" data-expert-scroller>
          <div class="mellow-expert-proof__track" data-expert-track>
            {%- assign fallback_letters = 'A,B,C,D,E,F' | split: ',' -%}
            {%- for card_index in (1..6) -%}
              {%- liquid
                assign image_key = 'card_' | append: card_index | append: '_image'
                assign letter_key = 'card_' | append: card_index | append: '_letter'
                assign role_key = 'card_' | append: card_index | append: '_role'
                assign quote_key = 'card_' | append: card_index | append: '_quote'
                assign name_key = 'card_' | append: card_index | append: '_name'
                assign title_key = 'card_' | append: card_index | append: '_title'
                assign fallback_letter = fallback_letters[forloop.index0]
                assign image_loading = 'lazy'
                if card_index <= 4
                  assign image_loading = 'eager'
                endif
              -%}

              <article
                class="mellow-expert-proof__card"
                data-expert-card
                data-expert-index="{{ forloop.index0 }}"
              >
                <div class="mellow-expert-proof__visual">
                  {%- if section.settings[image_key] != blank -%}
                    {{ section.settings[image_key] | image_url: width: 1000 | image_tag: decoding: 'async', class: 'mellow-expert-proof__image', loading: image_loading, sizes: '(min-width: 990px) 23vw, (min-width: 750px) 44vw, 84vw', widths: '320, 480, 640, 800, 1000', alt: section.settings[name_key] }}
                  {%- else -%}
                    <span class="mellow-expert-proof__letter" aria-hidden="true">{{ section.settings[letter_key] | default: fallback_letter | escape }}</span>
                  {%- endif -%}
                </div>
                <div class="mellow-expert-proof__content">
                  <span class="mellow-expert-proof__role">{{ section.settings[role_key] | escape }}</span>
                  <p class="mellow-card-copy mellow-expert-proof__quote">{{ section.settings[quote_key] | escape }}</p>
                  <div class="mellow-expert-proof__person">
                    <h3 class="mellow-card-title mellow-expert-proof__name">{{ section.settings[name_key] | escape }}</h3>
                    <p class="mellow-card-copy mellow-expert-proof__title">{{ section.settings[title_key] | default: section.settings[role_key] | escape }}</p>
                  </div>
                </div>
              </article>
            {%- endfor -%}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const initExpertProof = (section) => {
      const releaseReadyHold = window.MellowReadyGate ? window.MellowReadyGate.hold() : null;

      const releaseSectionReady = () => {
        if (section) {
          section.classList.add('is-mellow-section-ready');
        }

        if (releaseReadyHold) {
          releaseReadyHold();
        }
      };

      if (!section || section.dataset.mellowExpertProofReady === 'true') {
        releaseSectionReady();
        return;
      }

      const scroller = section.querySelector('[data-expert-scroller]');
      const track = section.querySelector('[data-expert-track]');
      const cards = Array.from(section.querySelectorAll('[data-expert-card]'));
      const prev = section.querySelector('[data-expert-prev]');
      const next = section.querySelector('[data-expert-next]');

      if (!scroller || !track || !cards.length || !prev || !next) {
        releaseSectionReady();
        return;
      }

      const reducedMotionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
      let controlFrame = null;
      let controlTimeout = null;
      let resizeObserver = null;

      const setReducedMotionClass = () => {
        section.classList.toggle('is-reduced-motion', reducedMotionQuery.matches);
      };

      const getTrackGap = () => {
        const trackStyles = window.getComputedStyle(track);
        return parseFloat(trackStyles.columnGap || trackStyles.gap) || 0;
      };

      const getCardPitch = () => {
        const firstCard = cards[0];
        const cardWidth = firstCard ? firstCard.getBoundingClientRect().width : 0;

        return cardWidth + getTrackGap();
      };

      const getMaxScroll = () => {
        return Math.max(0, scroller.scrollWidth - scroller.clientWidth);
      };

      const setButtonState = (button, disabled) => {
        button.disabled = disabled;
        button.setAttribute('aria-disabled', disabled ? 'true' : 'false');
      };

      const updateControls = () => {
        const maxScroll = getMaxScroll();
        const atStart = scroller.scrollLeft <= 1;
        const atEnd = maxScroll <= 1 || scroller.scrollLeft >= maxScroll - 1;

        setButtonState(prev, atStart);
        setButtonState(next, atEnd);
      };

      const scheduleControlUpdate = () => {
        if (controlFrame) {
          return;
        }

        controlFrame = window.requestAnimationFrame(() => {
          controlFrame = null;
          updateControls();
        });
      };

      const scrollByCard = (direction) => {
        const cardPitch = getCardPitch();

        if (cardPitch <= 0) {
          return;
        }

        scroller.scrollBy({
          left: cardPitch * direction,
          behavior: reducedMotionQuery.matches ? 'auto' : 'smooth'
        });

        scheduleControlUpdate();

        if (controlTimeout) {
          window.clearTimeout(controlTimeout);
        }

        controlTimeout = window.setTimeout(updateControls, reducedMotionQuery.matches ? 0 : 360);
      };

      const handleScroll = () => {
        scheduleControlUpdate();
      };

      const handleResize = () => {
        updateControls();
      };

      const handleMotionPreferenceChange = () => {
        setReducedMotionClass();
        updateControls();
      };

      const handleNextClick = () => scrollByCard(1);
      const handlePrevClick = () => scrollByCard(-1);

      next.addEventListener('click', handleNextClick);
      prev.addEventListener('click', handlePrevClick);
      scroller.addEventListener('scroll', handleScroll, { passive: true });

      setReducedMotionClass();

      if ('ResizeObserver' in window) {
        resizeObserver = new ResizeObserver(handleResize);
        resizeObserver.observe(track);
        resizeObserver.observe(scroller);
      } else {
        window.addEventListener('resize', handleResize);
      }

      if (typeof reducedMotionQuery.addEventListener === 'function') {
        reducedMotionQuery.addEventListener('change', handleMotionPreferenceChange);
      } else if (typeof reducedMotionQuery.addListener === 'function') {
        reducedMotionQuery.addListener(handleMotionPreferenceChange);
      }

      section.dataset.mellowExpertProofReady = 'true';

      window.requestAnimationFrame(() => {
        scroller.scrollLeft = 0;
        updateControls();
        releaseSectionReady();
      });

      section.mellowExpertProofCleanup = () => {
        if (controlFrame) {
          window.cancelAnimationFrame(controlFrame);
        }

        if (controlTimeout) {
          window.clearTimeout(controlTimeout);
        }

        scroller.removeEventListener('scroll', handleScroll);
        next.removeEventListener('click', handleNextClick);
        prev.removeEventListener('click', handlePrevClick);

        if (!resizeObserver) {
          window.removeEventListener('resize', handleResize);
        }

        if (typeof reducedMotionQuery.removeEventListener === 'function') {
          reducedMotionQuery.removeEventListener('change', handleMotionPreferenceChange);
        } else if (typeof reducedMotionQuery.removeListener === 'function') {
          reducedMotionQuery.removeListener(handleMotionPreferenceChange);
        }

        if (resizeObserver) {
          resizeObserver.disconnect();
        }
      };
    };

    const initCurrentSection = () => {
      initExpertProof(document.getElementById('MellowExpertProof-{{ section.id }}'));
    };

    initCurrentSection();

    document.addEventListener('shopify:section:load', (event) => {
      initExpertProof(event.target.querySelector('[data-mellow-expert-proof]'));
    });

    document.addEventListener('shopify:section:unload', (event) => {
      const section = event.target.querySelector('[data-mellow-expert-proof]');

      if (section && typeof section.mellowExpertProofCleanup === 'function') {
        section.mellowExpertProofCleanup();
      }
    });
  })();
</script>

{% schema %}
{
  "name": "Mellow expert proof",
  "tag": "section",
  "class": "section section-mellow-expert-proof",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "PRACTITIONER-BACKED PROOF"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Trusted by practitioners|who understand reflux."
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Subhead",
      "default": "Real-world guidance from digestive health|and wellness practitioners."
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "Shop Mellow"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    },
    {
      "type": "header",
      "content": "Card 1"
    },
    {
      "type": "image_picker",
      "id": "card_1_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_1_letter",
      "label": "Letter",
      "default": "A"
    },
    {
      "type": "text",
      "id": "card_1_name",
      "label": "Name",
      "default": "Noah R."
    },
    {
      "type": "text",
      "id": "card_1_role",
      "label": "Role",
      "default": "WELLNESS PRACTITIONER"
    },
    {
      "type": "text",
      "id": "card_1_title",
      "label": "Title",
      "default": "Wellness Practitioner"
    },
    {
      "type": "textarea",
      "id": "card_1_quote",
      "label": "Quote",
      "default": "“Most of my clients have been chasing reflux with antacids for years and never feel ahead of it. The raft-forming mechanism is genuinely different — and when something is this easy to use, people actually stick with it.”"
    },
    {
      "type": "header",
      "content": "Card 2"
    },
    {
      "type": "image_picker",
      "id": "card_2_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_2_letter",
      "label": "Letter",
      "default": "B"
    },
    {
      "type": "text",
      "id": "card_2_name",
      "label": "Name",
      "default": "Priya S."
    },
    {
      "type": "text",
      "id": "card_2_role",
      "label": "Role",
      "default": "FUNCTIONAL NUTRITIONIST"
    },
    {
      "type": "text",
      "id": "card_2_title",
      "label": "Title",
      "default": "Functional Nutritionist"
    },
    {
      "type": "textarea",
      "id": "card_2_quote",
      "label": "Quote",
      "default": "“I’m particular about what I recommend. Sodium alginate has decades of published research behind it, and Mellow’s formula is clean — no ingredients I’d question for daily use. That matters more than the marketing.”"
    },
    {
      "type": "header",
      "content": "Card 3"
    },
    {
      "type": "image_picker",
      "id": "card_3_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_3_letter",
      "label": "Letter",
      "default": "C"
    },
    {
      "type": "text",
      "id": "card_3_name",
      "label": "Name",
      "default": "Dr. Marcus Bell"
    },
    {
      "type": "text",
      "id": "card_3_role",
      "label": "Role",
      "default": "DIGESTIVE HEALTH ADVISOR"
    },
    {
      "type": "text",
      "id": "card_3_title",
      "label": "Title",
      "default": "Digestive Health Advisor"
    },
    {
      "type": "textarea",
      "id": "card_3_quote",
      "label": "Quote",
      "default": "“Alginate raft therapy is well-documented in the GI literature. What’s been missing is a format designed for real daily use. Mellow closes that gap — the science is solid and the delivery actually supports consistency.”"
    },
    {
      "type": "header",
      "content": "Card 4"
    },
    {
      "type": "image_picker",
      "id": "card_4_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_4_letter",
      "label": "Letter",
      "default": "D"
    },
    {
      "type": "text",
      "id": "card_4_name",
      "label": "Name",
      "default": "Talia W."
    },
    {
      "type": "text",
      "id": "card_4_role",
      "label": "Role",
      "default": "WELLNESS CLINICIAN"
    },
    {
      "type": "text",
      "id": "card_4_title",
      "label": "Title",
      "default": "Wellness Clinician"
    },
    {
      "type": "textarea",
      "id": "card_4_quote",
      "label": "Quote",
      "default": "“Consistency is the hardest part of any routine. Mellow is simple enough to take an evening without feeling like a chore — that’s exactly why I think people love it.”"
    },
    {
      "type": "header",
      "content": "Card 5"
    },
    {
      "type": "image_picker",
      "id": "card_5_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_5_letter",
      "label": "Letter",
      "default": "E"
    },
    {
      "type": "text",
      "id": "card_5_name",
      "label": "Name",
      "default": "Dr. Marcus Bell"
    },
    {
      "type": "text",
      "id": "card_5_role",
      "label": "Role",
      "default": "Digestive Health Advisor"
    },
    {
      "type": "text",
      "id": "card_5_title",
      "label": "Title",
      "default": "Digestive Health Advisor"
    },
    {
      "type": "textarea",
      "id": "card_5_quote",
      "label": "Quote",
      "default": "“Alginate raft therapy is well-documented in the GI literature. What's been missing is a format designed for real daily use. Mellow closes that gap — the science is solid and the delivery actually supports consistency.”"
    },
    {
      "type": "header",
      "content": "Card 6"
    },
    {
      "type": "image_picker",
      "id": "card_6_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_6_letter",
      "label": "Letter",
      "default": "F"
    },
    {
      "type": "text",
      "id": "card_6_name",
      "label": "Name",
      "default": "Talia W."
    },
    {
      "type": "text",
      "id": "card_6_role",
      "label": "Role",
      "default": "Wellness Clinician"
    },
    {
      "type": "text",
      "id": "card_6_title",
      "label": "Title",
      "default": "Wellness Clinician"
    },
    {
      "type": "textarea",
      "id": "card_6_quote",
      "label": "Quote",
      "default": "“Consistency is the hardest part of any wellness routine. Mellow is simple enough to fit into an evening without feeling clinical — which is exactly why I think people keep using it.”"
    }
  ],
  "presets": [
    {
      "name": "Mellow expert proof"
    }
  ]
}
{% endschema %}
