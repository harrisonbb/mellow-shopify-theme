{% comment %}
  Mellow Expert Proof section.
  Built on the global Mellow section system with a simple scroll row.
{% endcomment %}

<style>
  #MellowExpertProof-{{ section.id }} {
    --mellow-orange: var(--mellow-orange-red, #D04A2A);
    --mellow-expert-proof-card-width: clamp(300px, 24vw, 380px);
    --mellow-expert-proof-card-height: clamp(430px, 38vw, 520px);
    --mellow-expert-proof-card-pad: clamp(18px, 1.4vw, 24px);
    --mellow-carousel-card-gap: clamp(24px, 2.4vw, 36px);
    background: var(--mellow-section-green, #003302);
    color: var(--mellow-cream, #FBFAF4);
    overflow-x: hidden;
    overflow-y: visible;
    padding-top: clamp(36px, 4vw, 64px);
    padding-bottom: clamp(36px, 4vw, 64px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
    grid-template-columns: minmax(0, .62fr) auto;
    margin-bottom: var(--mellow-section-content-gap-compact);
    align-items: start;
    gap: clamp(18px, 2.4vw, 36px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header {
    max-width: 760px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-eyebrow {
    margin-bottom: var(--mellow-section-header-gap);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__heading {
    max-width: 680px;
    color: var(--mellow-cream, #FBFAF4);
    font-size: clamp(38px, 3.3vw, 52px);
    text-wrap: balance;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__text {
    max-width: 500px;
    margin-top: var(--mellow-section-heading-gap);
    font-size: clamp(15px, 1.05vw, 18px);
    line-height: 1.32;
    color: rgba(251, 250, 244, 0.86);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__actions {
    align-self: start;
    justify-self: end;
    display: flex;
    align-items: center;
    gap: clamp(12px, 1vw, 16px);
    width: auto;
    padding-top: clamp(4px, .5vw, 8px);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__cta {
    --mellow-section-cta-bg: var(--mellow-cream, #FBFAF4);
    --mellow-section-cta-border: var(--mellow-cream, #FBFAF4);
    --mellow-section-cta-color: var(--mellow-forest, #003302);
    --mellow-section-cta-hover-bg: var(--mellow-orange-red, #D04A2A);
    --mellow-section-cta-hover-border: var(--mellow-orange-red, #D04A2A);
    --mellow-section-cta-hover-color: var(--mellow-cream, #FBFAF4);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrows {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: clamp(44px, 3.25vw, 52px);
    height: clamp(44px, 3.25vw, 52px);
    padding: 0;
    border: 1px solid rgba(251, 250, 244, 0.42);
    border-radius: 999px;
    background: rgba(251, 250, 244, 0.12);
    color: var(--mellow-cream, #FBFAF4);
    cursor: pointer;
    box-shadow: 0 14px 28px rgba(0, 51, 2, 0.07);
    transition: background-color 180ms ease, border-color 180ms ease, color 180ms ease;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:not(:disabled):hover,
  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:not(:disabled):focus-visible {
    border-color: var(--mellow-cream, #FBFAF4);
    background: var(--mellow-cream, #FBFAF4);
    color: var(--mellow-forest, #003302);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow:disabled {
    cursor: default;
    opacity: 0.42;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__arrow svg {
    width: 16px;
    height: 16px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__bleed {
    width: 100vw;
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
    overflow-x: hidden;
    overflow-y: visible;
  }

  #MellowExpertProof-{{ section.id }}.is-reduced-motion .mellow-expert-proof__scroller {
    overflow-x: auto;
    scroll-behavior: auto;
  }

  #MellowExpertProof-{{ section.id }}.is-reduced-motion .mellow-expert-proof__track {
    transform: none !important;
    will-change: auto;
  }

  #MellowExpertProof-{{ section.id }}.is-reduced-motion .mellow-expert-proof__card-set[data-expert-card-set-duplicate] {
    display: none;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__scroller {
    width: 100%;
    overflow-x: hidden;
    overflow-y: visible;
    scrollbar-width: none;
    box-sizing: border-box;
    padding-left: var(--mellow-section-axis-pad);
    padding-right: var(--mellow-section-axis-pad);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__scroller::-webkit-scrollbar {
    display: none;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__track {
    display: flex;
    gap: var(--mellow-carousel-card-gap);
    align-items: stretch;
    width: max-content;
    max-width: none;
    will-change: transform;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__card-set {
    display: flex;
    gap: var(--mellow-carousel-card-gap);
    align-items: stretch;
    flex: 0 0 auto;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__card {
    position: relative;
    flex: 0 0 var(--mellow-expert-proof-card-width);
    width: var(--mellow-expert-proof-card-width);
    display: flex;
    flex-direction: column;
    height: var(--mellow-expert-proof-card-height);
    min-height: 0;
    min-width: 0;
    overflow: hidden;
    isolation: isolate;
    border-radius: min(var(--mellow-radius-card), 28px);
    border: 1px solid rgba(251, 250, 244, 0.18);
    background:
      radial-gradient(circle at 22% 20%, rgba(208, 74, 42, 0.28), transparent 30%),
      radial-gradient(circle at 76% 30%, rgba(61, 107, 140, 0.28), transparent 32%),
      linear-gradient(135deg, #F9E1BE 0%, #DCE4DF 52%, #E49D75 100%);
    box-shadow:
      0 22px 48px rgba(0, 18, 1, 0.24),
      0 1px 0 rgba(255, 255, 255, 0.22) inset;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual {
    position: absolute;
    inset: 0;
    z-index: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-radius: inherit;
    background:
      radial-gradient(circle at 22% 20%, rgba(208, 74, 42, 0.34), transparent 28%),
      radial-gradient(circle at 76% 30%, rgba(61, 107, 140, 0.34), transparent 30%),
      linear-gradient(135deg, #F9E1BE 0%, #DCE4DF 52%, #E49D75 100%);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual::before,
  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual::after {
    content: "";
    position: absolute;
    pointer-events: none;
    border-radius: 999px;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual::before {
    inset: 0;
    z-index: 2;
    border-radius: inherit;
    background:
      linear-gradient(180deg, rgba(0, 51, 2, 0.02) 0%, rgba(0, 28, 1, 0.08) 36%, rgba(0, 18, 1, 0.74) 100%),
      linear-gradient(0deg, rgba(0, 22, 1, 0.24), transparent 58%);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__visual::after {
    inset: auto 0 0;
    z-index: 2;
    height: 46%;
    border-radius: 0;
    background: linear-gradient(180deg, rgba(0, 31, 1, 0) 0%, rgba(0, 24, 1, 0.72) 44%, rgba(0, 18, 1, 0.94) 100%);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__image {
    position: absolute;
    inset: 0;
    z-index: 1;
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center 38%;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__letter {
    position: relative;
    z-index: 2;
    color: rgba(255, 252, 244, 0.84);
    font-family: var(--mellow-font-display);
    display: none;
    font-size: clamp(66px, 7vw, 108px);
    font-weight: 650;
    line-height: .78;
    letter-spacing: -0.08em;
    text-shadow: 0 18px 38px rgba(0, 51, 2, 0.18);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__content {
    position: absolute;
    inset: auto 0 0;
    z-index: 3;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    min-height: 38%;
    padding: var(--mellow-expert-proof-card-pad);
    padding-top: clamp(42px, 5vw, 72px);
    border-radius: 0 0 min(var(--mellow-radius-card), 28px) min(var(--mellow-radius-card), 28px);
    background:
      radial-gradient(circle at 90% 10%, rgba(208, 74, 42, 0.16), transparent 36%),
      linear-gradient(180deg, rgba(0, 31, 1, 0) 0%, rgba(0, 24, 1, 0.78) 38%, rgba(0, 18, 1, 0.96) 100%);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__role {
    display: block;
    margin: 0 0 clamp(6px, .55vw, 8px);
    color: var(--mellow-orange, #D04A2A);
    font-size: clamp(10px, .58vw, 11px);
    font-weight: var(--mellow-eyebrow-weight, 700);
    line-height: var(--mellow-eyebrow-line-height, 1);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__quote {
    margin: 0;
    color: var(--mellow-cream, #FBFAF4);
    font-size: clamp(15px, 0.95vw, 18px);
    line-height: 1.14;
    letter-spacing: -0.026em;
    text-wrap: balance;
    text-shadow: 0 2px 18px rgba(0, 18, 1, 0.36);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__person {
    margin-top: clamp(12px, 1vw, 16px);
    padding-top: clamp(10px, .9vw, 14px);
    border-top: 1px solid rgba(251, 250, 244, 0.22);
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__name {
    color: var(--mellow-cream, #FBFAF4);
    font-size: clamp(16px, 1vw, 19px);
    line-height: 1.02;
  }

  #MellowExpertProof-{{ section.id }} .mellow-expert-proof__title {
    margin-top: 3px;
    color: rgba(251, 250, 244, 0.72);
    font-size: clamp(12px, .86vw, 14px);
    line-height: 1.22;
  }

  @media screen and (max-width: 989px) {
    #MellowExpertProof-{{ section.id }} {
      --mellow-expert-proof-card-width: clamp(340px, 54vw, 440px);
      --mellow-expert-proof-card-height: clamp(460px, 62vw, 540px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
      grid-template-columns: 1fr;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__actions,
    #MellowExpertProof-{{ section.id }} .mellow-section-cta-wrap {
      justify-self: start;
      justify-content: flex-start;
    }
  }

  @media screen and (max-width: 749px) {
    #MellowExpertProof-{{ section.id }} {
      --mellow-expert-proof-card-width: min(82vw, 440px);
      --mellow-expert-proof-card-height: clamp(460px, 118vw, 560px);
      --mellow-expert-proof-card-pad: clamp(18px, 5.5vw, 24px);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__header-grid {
      gap: 18px;
      margin-bottom: var(--mellow-section-content-gap-compact);
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__actions {
      flex-wrap: wrap;
      width: 100%;
    }

    #MellowExpertProof-{{ section.id }} .mellow-expert-proof__content {
      min-height: 42%;
      padding: var(--mellow-expert-proof-card-pad);
      padding-top: clamp(38px, 12vw, 58px);
    }
  }
</style>

<section id="MellowExpertProof-{{ section.id }}" class="mellow-section-standard mellow-expert-proof color-{{ section.settings.color_scheme }}" data-mellow-expert-proof>
  <div class="mellow-section-axis">
    <div class="mellow-section-header-grid mellow-expert-proof__header-grid">
      <div class="mellow-section-header mellow-expert-proof__header">
        {%- if section.settings.eyebrow != blank -%}
          <p class="mellow-eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}

        {%- if section.settings.heading != blank -%}
          {%- assign heading_lines = section.settings.heading | escape | split: '|' -%}
          <h2 class="mellow-section-heading mellow-expert-proof__heading">
            {%- for line in heading_lines -%}
              {{ line }}{%- unless forloop.last -%}<br>{%- endunless -%}
            {%- endfor -%}
          </h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <p class="mellow-section-text mellow-expert-proof__text">{{ section.settings.text | escape }}</p>
        {%- endif -%}
      </div>

      <div class="mellow-section-actions mellow-expert-proof__actions">
        {%- if section.settings.cta_label != blank -%}
          <div class="mellow-section-cta-wrap">
            <a class="mellow-section-cta mellow-expert-proof__cta" href="{{ section.settings.cta_link | default: '#' }}">
              <span class="mellow-section-cta__label">{{ section.settings.cta_label | escape }}</span>
              <span class="mellow-section-cta__arrow" aria-hidden="true">→</span>
            </a>
          </div>
        {%- endif -%}

        <div class="mellow-expert-proof__arrows" aria-label="Expert proof scroll controls">
          <button class="mellow-expert-proof__arrow" type="button" aria-label="Previous expert quote" aria-disabled="false" data-expert-prev>
            <svg viewBox="0 0 16 16" fill="none" aria-hidden="true" focusable="false">
              <path d="M10 3.5 5.5 8 10 12.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <button class="mellow-expert-proof__arrow" type="button" aria-label="Next expert quote" data-expert-next>
            <svg viewBox="0 0 16 16" fill="none" aria-hidden="true" focusable="false">
              <path d="m6 3.5 4.5 4.5L6 12.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div class="mellow-expert-proof__bleed">
      <div class="mellow-expert-proof__scroller" data-expert-scroller>
        <div class="mellow-expert-proof__track" data-expert-track>
          {%- for card_loop in (1..3) -%}
            {%- assign is_original_set = false -%}
            {%- if card_loop == 2 -%}
              {%- assign is_original_set = true -%}
            {%- endif -%}

            <div
              class="mellow-expert-proof__card-set"
              {% if is_original_set %}
                data-expert-card-set
              {% else %}
                data-expert-card-set-duplicate
                aria-hidden="true"
              {% endif %}
            >
              {%- for card_index in (1..6) -%}
                {%- liquid
                  assign image_key = 'card_' | append: card_index | append: '_image'
                  assign letter_key = 'card_' | append: card_index | append: '_letter'
                  assign role_key = 'card_' | append: card_index | append: '_role'
                  assign quote_key = 'card_' | append: card_index | append: '_quote'
                  assign name_key = 'card_' | append: card_index | append: '_name'
                  assign fallback_letters = 'A,B,C,D,E,F' | split: ','
                  assign fallback_letter = fallback_letters[forloop.index0]
                  assign image_loading = 'lazy'
                  if card_index <= 3 and card_loop <= 2
                    assign image_loading = 'eager'
                  endif
                -%}

                <article
                  class="mellow-expert-proof__card"
                  {% if is_original_set %}
                    data-expert-card
                  {% endif %}
                  data-expert-index="{{ forloop.index0 }}"
                >
                  <div class="mellow-expert-proof__visual">
                    {%- if section.settings[image_key] != blank -%}
                      {{ section.settings[image_key] | image_url: width: 1200 | image_tag: decoding: 'async', class: 'mellow-expert-proof__image', loading: image_loading, sizes: '(min-width: 1584px) 380px, (min-width: 1250px) 24vw, (min-width: 990px) 300px, (min-width: 815px) 54vw, (min-width: 750px) 340px, 82vw', widths: '360, 520, 700, 900, 1200', alt: section.settings[name_key] }}
                    {%- endif -%}
                    <span class="mellow-expert-proof__letter" aria-hidden="true">{{ section.settings[letter_key] | default: fallback_letter | escape }}</span>
                  </div>
                  <div class="mellow-expert-proof__content">
                    <span class="mellow-expert-proof__role">{{ section.settings[role_key] | escape }}</span>
                    <p class="mellow-card-copy mellow-expert-proof__quote">{{ section.settings[quote_key] | escape }}</p>
                    <div class="mellow-expert-proof__person">
                      <h3 class="mellow-card-title mellow-expert-proof__name">{{ section.settings[name_key] | escape }}</h3>
                      <p class="mellow-card-copy mellow-expert-proof__title">{{ section.settings[role_key] | escape }}</p>
                    </div>
                  </div>
                </article>
              {%- endfor -%}
            </div>
          {%- endfor -%}
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const initExpertProof = (section) => {
      const releaseReadyHold = window.MellowReadyGate ? window.MellowReadyGate.hold() : null;

      const releaseSectionReady = () => {
        if (section) {
          section.classList.add('is-mellow-section-ready');
        }

        if (releaseReadyHold) {
          releaseReadyHold();
        }
      };

      if (!section || section.dataset.mellowExpertProofReady === 'true') {
        releaseSectionReady();
        return;
      }

      const scroller = section.querySelector('[data-expert-scroller]');
      const track = section.querySelector('[data-expert-track]');
      const cardSet = section.querySelector('[data-expert-card-set]');
      const cards = cardSet ? Array.from(cardSet.querySelectorAll('[data-expert-card]')) : [];
      const prev = section.querySelector('[data-expert-prev]');
      const next = section.querySelector('[data-expert-next]');

      if (!scroller || !track || !cardSet || !cards.length || !prev || !next) {
        releaseSectionReady();
        return;
      }

      const marqueeDuration = 84000;
      const nudgeDuration = 420;
      const reducedMotionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
      let setWidth = 0;
      let cardWidth = 0;
      let cardPitch = 0;
      let offset = 0;
      let animationFrame = null;
      let nudgeFrame = null;
      let lastFrameTime = null;
      let resizeObserver = null;
      let isReducedMotion = reducedMotionQuery.matches;

      const setReducedMotionState = () => {
        section.classList.toggle('is-reduced-motion', isReducedMotion);
        prev.disabled = false;
        next.disabled = false;
        prev.setAttribute('aria-disabled', 'false');
        next.setAttribute('aria-disabled', 'false');
      };

      const applySafeOffset = () => {
        if (setWidth <= 0) {
          return;
        }

        while (offset <= setWidth * -1.5) {
          offset += setWidth;
        }

        while (offset >= setWidth * -0.5) {
          offset -= setWidth;
        }
      };

      const updateTrackPosition = () => {
        track.style.transform = isReducedMotion ? 'none' : `translate3d(${offset}px, 0, 0)`;
      };

      const stopNudge = () => {
        if (nudgeFrame) {
          cancelAnimationFrame(nudgeFrame);
          nudgeFrame = null;
        }
      };

      const stopMarquee = () => {
        if (animationFrame) {
          cancelAnimationFrame(animationFrame);
          animationFrame = null;
        }

        stopNudge();
        lastFrameTime = null;
      };

      const measureCarousel = () => {
        const trackStyles = window.getComputedStyle(track);
        const cardSetStyles = window.getComputedStyle(cardSet);
        const trackGap = parseFloat(trackStyles.columnGap || trackStyles.gap) || 0;
        const cardGap = parseFloat(cardSetStyles.columnGap || cardSetStyles.gap) || 0;
        const firstCard = cards[0];

        cardWidth = firstCard ? firstCard.getBoundingClientRect().width : 0;
        cardPitch = cardWidth + cardGap;
        setWidth = cardSet.getBoundingClientRect().width + trackGap;

        if (setWidth <= 0) {
          return;
        }

        offset = isReducedMotion ? 0 : setWidth * -1;
        track.style.setProperty('--expert-track-width', `${setWidth}px`);
        updateTrackPosition();
      };

      const animate = (timestamp) => {
        if (isReducedMotion) {
          stopMarquee();
          updateTrackPosition();
          return;
        }

        if (setWidth > 0 && !nudgeFrame) {
          if (lastFrameTime === null) {
            lastFrameTime = timestamp;
          }

          const elapsed = timestamp - lastFrameTime;
          const pixelsPerMillisecond = setWidth / marqueeDuration;

          offset -= pixelsPerMillisecond * elapsed;
          applySafeOffset();
          updateTrackPosition();
          lastFrameTime = timestamp;
        } else if (nudgeFrame) {
          lastFrameTime = timestamp;
        }

        animationFrame = requestAnimationFrame(animate);
      };

      const startMarquee = () => {
        if (isReducedMotion || animationFrame) {
          return;
        }

        animationFrame = requestAnimationFrame(animate);
      };

      const easeInOutCubic = (progress) => {
        return progress < 0.5
          ? 4 * progress * progress * progress
          : 1 - Math.pow(-2 * progress + 2, 3) / 2;
      };

      const nudgeMarquee = (direction) => {
        if (cardPitch <= 0) {
          return;
        }

        if (isReducedMotion) {
          scroller.scrollBy({ left: cardPitch * direction, behavior: 'auto' });
          return;
        }

        stopNudge();

        const startOffset = offset;
        const targetOffset = startOffset - cardPitch * direction;
        const startTime = performance.now();

        const step = (timestamp) => {
          const progress = Math.min(1, (timestamp - startTime) / nudgeDuration);
          const easedProgress = easeInOutCubic(progress);

          offset = startOffset + (targetOffset - startOffset) * easedProgress;
          applySafeOffset();
          updateTrackPosition();

          if (progress < 1) {
            nudgeFrame = requestAnimationFrame(step);
            return;
          }

          nudgeFrame = null;
          lastFrameTime = null;
        };

        nudgeFrame = requestAnimationFrame(step);
      };

      const normalizeWheelDelta = (delta, deltaMode) => {
        if (deltaMode === WheelEvent.DOM_DELTA_LINE) {
          return delta * 16;
        }

        if (deltaMode === WheelEvent.DOM_DELTA_PAGE) {
          return delta * scroller.clientWidth;
        }

        return delta;
      };

      const handleWheel = (event) => {
        if (isReducedMotion) {
          return;
        }

        const horizontalDelta = normalizeWheelDelta(event.deltaX, event.deltaMode);
        const verticalDelta = normalizeWheelDelta(event.deltaY, event.deltaMode);
        const isIntentionalHorizontalScroll = Math.abs(horizontalDelta) > Math.abs(verticalDelta) * 1.05;

        if (!isIntentionalHorizontalScroll || setWidth <= 0) {
          return;
        }

        event.preventDefault();
        stopNudge();
        offset -= horizontalDelta;
        applySafeOffset();
        updateTrackPosition();
        lastFrameTime = null;
      };

      const handleResize = () => {
        lastFrameTime = null;
        measureCarousel();
      };

      const handleMotionPreferenceChange = (event) => {
        isReducedMotion = event.matches;
        setReducedMotionState();
        stopMarquee();
        measureCarousel();
        startMarquee();
      };

      const handleNextClick = () => nudgeMarquee(1);
      const handlePrevClick = () => nudgeMarquee(-1);

      next.addEventListener('click', handleNextClick);
      prev.addEventListener('click', handlePrevClick);
      scroller.addEventListener('wheel', handleWheel, { passive: false });

      setReducedMotionState();
      measureCarousel();

      if ('ResizeObserver' in window) {
        resizeObserver = new ResizeObserver(handleResize);
        resizeObserver.observe(cardSet);
        resizeObserver.observe(scroller);
      } else {
        window.addEventListener('resize', handleResize);
      }

      if (typeof reducedMotionQuery.addEventListener === 'function') {
        reducedMotionQuery.addEventListener('change', handleMotionPreferenceChange);
      } else if (typeof reducedMotionQuery.addListener === 'function') {
        reducedMotionQuery.addListener(handleMotionPreferenceChange);
      }

      startMarquee();
      section.dataset.mellowExpertProofReady = 'true';
      window.requestAnimationFrame(releaseSectionReady);

      section.mellowExpertProofCleanup = () => {
        stopMarquee();
        scroller.removeEventListener('wheel', handleWheel);
        next.removeEventListener('click', handleNextClick);
        prev.removeEventListener('click', handlePrevClick);

        if (!resizeObserver) {
          window.removeEventListener('resize', handleResize);
        }

        if (typeof reducedMotionQuery.removeEventListener === 'function') {
          reducedMotionQuery.removeEventListener('change', handleMotionPreferenceChange);
        } else if (typeof reducedMotionQuery.removeListener === 'function') {
          reducedMotionQuery.removeListener(handleMotionPreferenceChange);
        }

        if (resizeObserver) {
          resizeObserver.disconnect();
        }
      };
    };

    const initCurrentSection = () => {
      initExpertProof(document.getElementById('MellowExpertProof-{{ section.id }}'));
    };

    initCurrentSection();

    document.addEventListener('shopify:section:load', (event) => {
      initExpertProof(event.target.querySelector('[data-mellow-expert-proof]'));
    });

    document.addEventListener('shopify:section:unload', (event) => {
      const section = event.target.querySelector('[data-mellow-expert-proof]');

      if (section && typeof section.mellowExpertProofCleanup === 'function') {
        section.mellowExpertProofCleanup();
      }
    });
  })();
</script>

{% schema %}
{
  "name": "Mellow expert proof",
  "tag": "section",
  "class": "section section-mellow-expert-proof",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "PRACTITIONER-BACKED PROOF"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Trusted by practitioners|who understand reflux."
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Subhead",
      "default": "Real-world guidance from digestive health and wellness practitioners."
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "Shop Mellow"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    },
    {
      "type": "header",
      "content": "Card 1"
    },
    {
      "type": "image_picker",
      "id": "card_1_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_1_letter",
      "label": "Letter",
      "default": "A"
    },
    {
      "type": "text",
      "id": "card_1_name",
      "label": "Name",
      "default": "Dr. Lena Ortiz"
    },
    {
      "type": "text",
      "id": "card_1_role",
      "label": "Role",
      "default": "Integrative Gastroenterologist"
    },
    {
      "type": "textarea",
      "id": "card_1_quote",
      "label": "Quote",
      "default": "“What I appreciate most is that Mellow doesn't suppress acid — it creates a physical barrier instead. For clients who want meaningful support without the concerns around long-term acid suppression, that distinction matters a lot.”"
    },
    {
      "type": "header",
      "content": "Card 2"
    },
    {
      "type": "image_picker",
      "id": "card_2_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_2_letter",
      "label": "Letter",
      "default": "B"
    },
    {
      "type": "text",
      "id": "card_2_name",
      "label": "Name",
      "default": "Maya C."
    },
    {
      "type": "text",
      "id": "card_2_role",
      "label": "Role",
      "default": "Registered Dietitian"
    },
    {
      "type": "textarea",
      "id": "card_2_quote",
      "label": "Quote",
      "default": "“For people who deal with reflux after meals, the format matters. A single pouch is easier to build into a routine than another bottle, tablet, or chalky liquid — and that makes the science more usable.”"
    },
    {
      "type": "header",
      "content": "Card 3"
    },
    {
      "type": "image_picker",
      "id": "card_3_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_3_letter",
      "label": "Letter",
      "default": "C"
    },
    {
      "type": "text",
      "id": "card_3_name",
      "label": "Name",
      "default": "Noah R."
    },
    {
      "type": "text",
      "id": "card_3_role",
      "label": "Role",
      "default": "Wellness Practitioner"
    },
    {
      "type": "textarea",
      "id": "card_3_quote",
      "label": "Quote",
      "default": "“Most of my clients have been chasing reflux with antacids for years and never feel ahead of it. The raft-forming mechanism is genuinely different — and when something is this easy to use, people actually stick with it.”"
    },
    {
      "type": "header",
      "content": "Card 4"
    },
    {
      "type": "image_picker",
      "id": "card_4_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_4_letter",
      "label": "Letter",
      "default": "D"
    },
    {
      "type": "text",
      "id": "card_4_name",
      "label": "Name",
      "default": "Priya S."
    },
    {
      "type": "text",
      "id": "card_4_role",
      "label": "Role",
      "default": "Functional Nutritionist"
    },
    {
      "type": "textarea",
      "id": "card_4_quote",
      "label": "Quote",
      "default": "“I'm particular about what I recommend. Sodium alginate has decades of published research behind it, and Mellow's formula is clean — no ingredients I'd question for daily use. That matters more than the marketing.”"
    },
    {
      "type": "header",
      "content": "Card 5"
    },
    {
      "type": "image_picker",
      "id": "card_5_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_5_letter",
      "label": "Letter",
      "default": "E"
    },
    {
      "type": "text",
      "id": "card_5_name",
      "label": "Name",
      "default": "Dr. Marcus Bell"
    },
    {
      "type": "text",
      "id": "card_5_role",
      "label": "Role",
      "default": "Digestive Health Advisor"
    },
    {
      "type": "textarea",
      "id": "card_5_quote",
      "label": "Quote",
      "default": "“Alginate raft therapy is well-documented in the GI literature. What's been missing is a format designed for real daily use. Mellow closes that gap — the science is solid and the delivery actually supports consistency.”"
    },
    {
      "type": "header",
      "content": "Card 6"
    },
    {
      "type": "image_picker",
      "id": "card_6_image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "card_6_letter",
      "label": "Letter",
      "default": "F"
    },
    {
      "type": "text",
      "id": "card_6_name",
      "label": "Name",
      "default": "Talia W."
    },
    {
      "type": "text",
      "id": "card_6_role",
      "label": "Role",
      "default": "Wellness Clinician"
    },
    {
      "type": "textarea",
      "id": "card_6_quote",
      "label": "Quote",
      "default": "“Consistency is the hardest part of any wellness routine. Mellow is simple enough to fit into an evening without feeling clinical — which is exactly why I think people keep using it.”"
    }
  ],
  "presets": [
    {
      "name": "Mellow expert proof"
    }
  ]
}
{% endschema %}
